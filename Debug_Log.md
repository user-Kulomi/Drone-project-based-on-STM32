# 一. 无人机SI24R1上电无法接收数据包

| 项目 | 内容 |
|---|---|
| 日期 | 2026-07-29 |
| 模块 | SI24R1 2.4G 无线通信 |
| 硬件 | STM32F103 + SI24R1 无线模块 |
| 软件环境 | Keil MDK / FreeRTOS |
| 故障状态 | 已修复 |

---

## 1. 问题概述

### 1.1 现象描述

无人机上电调试时，SI24R1 持续无法接收遥控器下发的数据包。程序编译无报错，硬件也没有明显异常，但无人机接收逻辑一直判定为空。
遥控器发送端日志显示数据包发送成功，无人机端却始终收不到有效数据。

## 2. 排查过程
### 2.1 尝试在无人机上用串口打印接收结果
```c
//调用SI24R1接收数据包：
Int_SI24R1_RxPacket(rx_buff);
if(strlen((char*)rx_buff) == 0)
{
    debug_printf("未接收到数据");
    return 1; //未接收到数据
}
debug_printf("接收到数据：%s", rx_buff);
return 0; //接收到数据
```
**结论：打印结果始终显示未接收到。**

### 2.2 怀疑是遥控器发送的数据包有问题，尝试在电脑上用串口打印接收结果
```c
void App_transmit_Data(void)
{
    //...
    uint8_t result = Int_SI24R1_TxPacket(Send_Data_Buffer);//发送数据包
    //检查发送结果
    if (result == 0)
    {
        //打印前3字节，验证帧头是否非0：
        debug_printf("发送成功：%d, %d, %d\r\n", Send_Data_Buffer[0],
        Send_Data_Buffer[1], Send_Data_Buffer[2]);
    }
    //...
}
```
**结论：显示发送成功。说明发送端没有问题，问题可能出在接收端。**

### 2.3 尝试将接收数据的判断依据改为判断接收函数返回值以修复问题
```c
uint8_t rec_res = Int_SI24R1_RxPacket(rx_buff);
if(rec_res == 1)
{
    debug_printf(":未接收到数据\r\n");
    return 1;
}
else
{
    debug_printf("接收到数据：%s", rx_buff);
    return 0;
}
```
**结论：仍然无法接收到数据，需要进一步排查问题。**

### 2.4 寻找出现问题的具体对象
尝试将无人机电断，观察遥控器发送结果，结果打印的日志显示无法发送，上电后遥控器又可以正常发送数据。考虑到SI24R1的一端发送数据后只有接收到来自另一端的ACK后，才能够正常发送。而无人机作为接收端，只有在接收到数据后硬件才会发送ACK，说明两端的硬件联通性不存在任何问题。于是问题就锁定在了无人机软件接收上。

**结论：问题大概率出在无人机软件接收上。**

## 3. 寻找解决方案
### 3.1 修复原本的发送模块存在的缺陷
发现原教程中接收数据包函数
```c
Int_SI24R1_RxPacket();
```
中的清除RX FIFO寄存器（接收到的数据）功能是调用写寄存器函数的方式实现的，即
```c
Int_SI24R1_Write_Reg(FLUSH_RX, 0xff);
```
上网查阅相关资料，发现这样会连续发送两个字节：0xE2 + 0xFF，原本只需发送单字节指令0xE2即可清空寄存器的，现在多出的0xFF会导致芯片误当做一条SPI指令。虽然单次影响不大，但频繁调用会打乱芯片内部 FIFO 状态，最终导致 RX FIFO 异常、数据无法正常写入。于是我使用了
```c
void Int_SI24R1_FlushRX(void)
{
    CS_LOW();
    SPI_RW(FLUSH_RX); // 仅发送指令，无额外数据
    CS_HIGH();
}
```
函数代替原函数，尝试解决问题。

**结论：问题仍然存在。**

### 3.2 尝试在切换模式时清空FIFO队列
又考虑到芯片上电 / 模式切换时，由于上电复位不彻底、模式频繁切换等各种因素，RX_FIFO 状态是不确定的，导致软件无法正常读取数据。于是我尝试在切换模式的函数中加入清空FIFO的代码，保证不会出现硬件接收到数据而软件读取数据异常的情况：
```c
//清空FIFO队列，防止接收数据时，FIFO队列中有残留数据导致接收失败；
Int_SI24R1_FlushTX();
Int_SI24R1_FlushRX();
Int_SI24R_WriteReg(SI24R1_WRITE_REG + 0x07, 0xFF);
```
**结论：经本次尝试后，无人机能正常接收数据，问题解决！**

## 4. 问题根因总结
SI24R1在收发模式频繁切换、上电复位不充分场景下，芯片内部RX FIFO状态机容易遗留异常标记。只单独刷新RX FIFO不足以重置芯片内部状态；必须在模式切换阶段同时清空TX、RX两端FIFO，彻底清除芯片内部残留状态，才能正常接收数据包。
另外补充一处原教程存在的不规范问题：使用strlen判断缓冲区有无数据并不严谨，二进制数据包不能依靠字符串长度判定接收结果，应当使用驱动函数返回值作为判断标准。

# 二. 两端连接成功后，飞机状态有概率异常

| 项目 | 内容 |
|---|---|
| 日期 | 2026-08-05 |
| 模块 | App_receive_data |
| 软件环境 | Keil MDK / FreeRTOS |
| 故障状态 | 已修复 |

---

## 1. 问题概述

### 1.1 现象描述

完成PID控制电机的基本框架后，在调试时，发现如果无人机在连接遥控器并解锁油门后，即使不进行任何操作，飞机状态也会在一段时间后自动由正常状态变为空闲状态，导致无法正常完成飞控任务。

## 2. 排查过程

### 2.1 尝试在无人机上用串口打印接收结果

在process_flight_state中的每个状态前加入

```c
debug_printf("进入对应状态");
```

结果发现飞机在连接成功后，先是变为正常状态，持续一段时间后来到了FAIL（故障状态），然后来到了空闲状态。按照飞机的状态机逻辑，这说明飞机与遥控器通信的过程中必定产生了失联的情况。

**结论：飞机与遥控器连接成功后失联**

### 2.2 观察飞机led灯闪烁指示判断具体原因

已知位于飞机前方的两个灯在连接成功后会常亮，连接失败则常闭。后两个灯在空闲状态会慢闪，正常状态会快闪。而在刚刚的调试过程中，前两个灯一直保持常亮，没有关闭的现象。后两个灯先是快闪，后慢闪，对应了前面所遇到的飞机达到正常状态后又变为空闲状态。考虑到对于后两个灯，人眼可能无法察觉瞬时的亮灭变化，所以用这个方法无法得到想要的结论，我决定直接开始排查失联的原因。

**结论：需要继续排查失联原因**

### 2.3 考虑能导致失联的原因

飞机与遥控器的连接状态是由remote_state枚举决定的：

```c
typedef enum
{
    REMOTE_CONNECT = 0,//连接状态
    REMOTE_DISCONNECT,//失联状态
}Remote_State;
```

而本程序中，能更改该枚举的值的地方一共有两处。分别是初始化时赋予的REMOTE_DISCONNECT，以及process_connect_state函数：

```c
/**
* @brief 处理遥控器的连接状态
* @param res: 接收数据结果。0表示接收成功，1表示接收失败
*/
void process_connect_state(uint8_t res)
{
    if(res == 0)
    {
        remote_state = REMOTE_CONNECT; //连接成功
        Try_count = 0; //连接成功，重置尝试连接次数
    }
    else
    {
        Try_count++; //增加尝试连接次数
        if(Try_count >= MAX_RETRY_CONNECT_COUNT)
        {
            remote_state = REMOTE_DISCONNECT; //连接失败
            Try_count = 0; //重置尝试连接次数
        }
    }
}
```

我在else分支里面的if分支开头处加入了debug_printf("DISCON!")，以测试是否是在通信过程中达到最大重试连接次数而导致的连接状态改变，发现果然如此。而如果飞机处于正常状态时，连接状态变为失联状态，飞机最终会变为空闲状态并保持不变，不论以后是否再次连接成功。这也解释了为什么灯会先快闪，一段时间后变为慢闪。

**结论：失联的原因为达到最大重试次数**

## 3. 寻找解决方案

### 3.1 思考为何会在连接成功后会重试连接失败

上网查阅相关信息得，双端无线通信时会产生丢包。按照本程序逻辑，每一次丢包都会尝试重连。而当重连次数达到设定的上限后，遥控器连接状态会变为失联状态，进而导致程序误判连接失败。

**结论：程序误判连接状态为失败状态**

### 3.2 解决误判问题

飞机为正常状态时，如果检测到失联，会直接跳转到故障->空闲状态。我尝试在跳转之前加入一个重连等待机制，即飞机在达到失联状态500ms后，连接状态仍为失联，就正式判定为失联。如果中途连接状态恢复到成功连接，就不更改飞机状态为故障：

```c
    static uint16_t disconnect_timer = 0;//失联计时数
    switch(flight_state)
    //...
        case NORMAL:
        {
            if(remote_data.fix_height == 1)
            {
                flight_state = FIX_HEIGHT; //收到切换定高状态指令，进入定高状态
                remote_data.fix_height = 0; //清除切换定高状态指令，避免重复进入定高分支导致运行异常
                disconnect_timer = 0;//清零失联计时数
            }
            else if(remote_state == REMOTE_DISCONNECT)//中途断开连接
            {
                disconnect_timer += 6;//每次判断连接状态时，若是失联状态，就让计时数+6（一个本任务周期：6ms）
                if(disconnect_timer >= 500 && remote_state == REMOTE_DISCONNECT)//失联持续500ms以上，判定为彻底断开连接
                {
                    flight_state = FAIL; //遥控器断开连接，进入故障状态
                    disconnect_timer = 0;//清零失联计时数
                }
            }
            else if(remote_state == REMOTE_CONNECT)//中途恢复连接
            {
                disconnect_timer = 0;//清零失联计时数
            }
            break;
    //...
```

在重新编译运行后，飞机再也没有出现过自动跳转到故障->空闲状态的情况，问题解决。

**结论：经本次尝试后，无人机始终能保持正常运行状态，问题解决！**

## 4. 问题根因总结

双端无线通信时难免会产生丢包现象。原教程的代码仅将最大重试连接次数当做连接失败的标志，而没有增加更严谨的重连等待逻辑，导致了程序在复现时可能会出现异常结果。其实本人在查询相关信息后也尝试过增加最大重试次数，但终究治标不治本，一段时间的丢包后飞机仍然会来到故障状态。再者，也不建议盲目加大最大重连次数，阈值过大的话，遥控器真的断电或失控时，系统反应太慢，有安全风险。配合重连等待逻辑才是更合理的方案。
